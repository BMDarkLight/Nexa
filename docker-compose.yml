version: "3.8"

networks:
  shared-net:
    name: multi-team-dev-deployment_shared-net
    external: true

services:
  backend:
    image: bmdarklight/nexa-api:latest
    command: sh -c "uvicorn api.main:app --host 0.0.0.0 --port ${API_PORT:-8000}"
    volumes:
      - .:/app
    ports:
      - "${API_PORT:-8000}:${API_PORT:-8000}"
    networks:
      - shared-net

  frontend:
    image: bmdarklight/nexa-ui:latest
    working_dir: /app/web
    command: sh -c "npm run dev -- -p ${UI_PORT:-8010}"
    volumes:
      - ./web:/app/web
    ports:
      - "${UI_PORT:-8010}:${UI_PORT:-8010}"
    networks:
      - shared-net
    depends_on:
      - backend